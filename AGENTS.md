# ProcureRPA Enterprise

## 项目定位

* 基于 Skyvern 的采购 RPA 演示级企业项目。
* 核心目标是让采购主流程真实跑通，不追求大型生产系统的完整度。
* 设计参考 `docs/procurerpa_enterprise_migration_plan.md`。
* 文档与实际代码不一致时，以真实代码和当前任务目标为准。

## 开发优先级

按照以下顺序进行开发和验收：

1. 当前任务要求的功能是否实现。
2. 功能是否接入真实调用链。
3. 主流程是否能够运行。
4. 是否存在数据错误、安全风险或流程阻断。
5. 代码是否便于理解和继续迭代。

只要核心功能完成、主流程可运行且不存在阻塞问题，即可认为当前任务完成。

不得因为低概率边界情况、代码风格、轻微重复、文档措辞、测试覆盖率或未来扩展性问题否定当前实现。

## 架构约束

* 优先复用 Skyvern 原生 Task、Workflow、Action 和浏览器执行链。
* 不新增第二套浏览器执行引擎。
* 不引入微服务、Kafka、复杂规则平台等非必要组件。
* 企业功能优先采用单体模块化实现。
* 优先使用最简单、最少修改、能够满足当前需求的方案。
* 不为假设中的未来需求提前设计复杂抽象。
* 不进行与当前任务无关的重构、清理或技术升级。

## 安全约束

* 密码、Token、供应商银行账户和敏感采购数据不得发送给 LLM。
* 金额、提交、删除、审批等关键决策必须由确定性规则控制。
* LLM 只能用于解释、字段映射和非决定性补充判断。
* 不得在代码、日志、测试或示例配置中写入真实密钥。

安全问题、数据损坏问题和主流程阻断问题必须修复。

## 修改规则

* 修改前检查真实调用方、路由注册、数据库模型和已有实现。
* 不根据 README 或计划文档推断功能已经实现。
* 只修改完成当前任务所必需的文件。
* 不顺带修复无关旧问题，不扩大任务范围。
* 已有功能可以复用时，不重复建设。
* 允许少量局部硬编码、简单条件判断或重复代码，只要不影响当前功能和后续迭代。
* 无法验证的内容应明确说明，但不因此否定已完成的代码。

## 问题分级

### 阻塞问题

必须在当前任务中解决：

* 核心流程无法运行。
* 代码无法启动、导入或执行。
* 关键接口返回错误结果。
* 数据可能被错误写入、覆盖或删除。
* 存在敏感信息泄露风险。
* 功能没有接入真实调用链。
* 当前修改导致最小相关测试失败。

### 非阻塞问题

默认不影响当前任务验收：

* 极低概率边界情况。
* 纯代码风格或命名问题。
* 轻微重复代码。
* 非关键类型检查、lint 或静态检查警告。
* 理论上的性能优化。
* 未来扩展性建议。
* 生产级异常恢复、高并发和高可用需求。
* 与当前功能无关的历史测试失败。

非阻塞问题可以记录为后续建议，但不得作为任务失败或返工理由。

## 测试与验收

* 每次实现只运行最小相关测试。
* 优先验证当前模块、真实调用链和核心主流程。
* 不要求运行与当前修改无关的完整测试集。
* 不追求 100% 测试覆盖率。
* 外部依赖不可用时，可以使用 Mock、局部测试、静态检查或调用链分析替代。
* 不因为 lint、格式化、注释或非关键警告拒绝验收。

验收只需确认：

1. 当前功能是否实现。
2. 是否接入真实调用链。
3. 主流程是否正常。
4. 是否存在阻塞问题。
5. 执行了哪些验证。

前四项满足时，应明确判定任务通过。

## 停止条件

满足以下条件后立即停止扩展当前任务：

* 当前需求已经实现。
* 主流程已经接通。
* 已完成最小相关验证。
* 不存在已知阻塞问题。
* 剩余问题均为非阻塞建议。

达到停止条件后，不再主动增加功能、扩大测试范围、重构相邻代码或处理低价值边角问题。

## 文档记录

每个 Day 完成后更新：

* `summary`：完成的核心功能和验证结果，内容按照finrpa的summary的写法。
* `code list`：新增或修改的关键文件，内容按照finrpa的code list的写法。
* 设计决策：重要架构选择。
* 踩坑记录：实际遇到并解决的问题。
* 后续建议：最多记录 1 至 3 个有价值的非阻塞问题。

## 最终输出

完成任务后简洁说明：

* 完成了什么。
* 修改了哪些关键文件。
* 如何验证。
* 是否存在阻塞问题。
* 哪些内容尚未验证。

不要罗列大量边界问题，不要输出冗长的自我审查过程。

当核心功能满足要求且不存在阻塞问题时，明确说明：

**当前任务已完成，可以进入下一阶段。**
